package demo.quarkus.temporal;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeTripResourceIT extends TripResourceTest {

    // Execute the same tests but in native mode.
}